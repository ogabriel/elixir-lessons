# is_number(1)
# i_n = &is_number/1
# i.n.(2)
# lista = [1, 2, 3]
# Enum.map(lista, fn (numero) -> numero * 2 end)
# Enum.map(lista, &multiplicar_por_dois/1)
# Enum.map(lista, &(&1 * 2))
# soma_2 = &(&1 + 2)
# soma_2.(6)
